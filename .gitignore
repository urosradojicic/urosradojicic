# Local preview harness — reproduces GitHub's README constraints for checking
# layout. Not part of the profile.
preview.html

# Intermediate from the portrait pipeline. The source photo is kept; the
# prepped grayscale is derived and can always be regenerated.
source/portrait-prepped.png

__pycache__/
*.pyc
